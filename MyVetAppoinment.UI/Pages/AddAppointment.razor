@page "/addappointment/{petId:guid}/{cabinetId:guid}"
@inject HttpClient Http
@inject NavigationManager NavigationManager
<link href="css/appointment.css" rel="stylesheet" />
@page "/data"
@using MyVetAppoinment.Shared.Domain;


<head>
    <link href="../css/shop.css" rel="stylesheet" />
</head>
<div class="cont">
    <div class="row"><h1> Pet</h1></div>


    <div class="row">
        <div class="modal-body">
            <EditForm Model="@model" OnValidSubmit="@OnSubmit">
                <DataAnnotationsValidator />
                <ValidationSummary />
                @*<div class="form-group">
                    <label for="Date"> Data: </label>
                    <input type="datetime-local" id="Date" class="form-control" @bind-Value="@model.StartTime"/>
                    <ValidationMessage For="@(() => model.StartTime)" />
                </div>*@
                <div class="form-group">
                <label for="Date"> Data si ora: </label>
                    <input type="datetime-local" id="Date" class="form-control" @bind-value="@model.StartTime" />
                    <ValidationMessage For="@(() => model.StartTime)" />
                </div>
               
                <div class="form-group">
                    <label for="Description"> Descriere: </label>
                    <InputText id="Description" class="form-control" @bind-Value="@model.Description" placeholder=" Enter  Description"></InputText>
                    <ValidationMessage For="@(() => model.Description)" />
                </div>
                <div class="form-group">
                   
                    <div class="col">
                        @if (Error != null)
                        {
                            <div> @Error</div>
                        }

                    </div>
                </div>
        
               
                <button type="submit" class="btn btn-primary">Next step</button>
                <a class="btn btn-outline-primary" @onclick="@Cancel">Close</a>
            </EditForm>
        </div>
    </div>





</div>




@*<RadzenTemplateForm TItem="Appointment" Data="@model" Submit=@OnSubmit>

    <div class="antet">
       <strong> PROGRAM:
        <br>
        Luni: 09:00-16:00 <br>
        Marti: 09:00-16:00 <br>
        Miercuri: 09:00-16:00 <br>
        Joi: 09:00-16:00 <br>
        Vineri: 09:00-16:00 <br>
        Sambata,Duminica: inchis <br>
        <br>
        </strong>
    </div>
    <div class="container">
    <div class="row" style="margin-bottom: 16px">
        <div class="col-md-3">
            <RadzenLabel Text="Alegeti data si ora:" />
        </div>
        <div class="col">


            <RadzenDatePicker Min="DateTime.Today" YearRange="2023:2024" @bind-Value="@model.StartTime" Name="Start" ShowTime="true" MinutesStep="30" />

            <RadzenRequiredValidator Text="Start is required" Component="Start" />
        </div>
    </div>

    <div class="row" style="margin-bottom: 16px">
        <div class="col-md-3">
            <RadzenLabel Text="Decriere" />
        </div>
        <div class="col">
            <RadzenTextBox @bind-Value="@model.Description" Name="Description" />
            <RadzenRequiredValidator Text="Description is required" Component="Description" />
        </div>
    </div>

    <div class="row">
        <div class="col-md-3"></div>
        <div class="col">
            <RadzenButton ButtonType="ButtonType.Submit" Text="Save" />
        </div>
    </div>
    <div class="row">
        <div class="col-md-3"></div>
        <div class="col">
            @if (error != null)
            {
                <div> @error</div>
            }

        </div>
    </div>
    </div>

</RadzenTemplateForm>
*@